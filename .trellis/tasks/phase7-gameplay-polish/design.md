# Design — Phase 7

Pipeline: audit → pick 3–5 highest-value problems → small tuning → playtest → lock. Do not change 20 variables at once.

## Evaluation order

Player experience > game feel > flow > fairness > camera > feedback > replay > code > perf.

## Sky signature

“果冻把你弹起来。看清落点再跳。高岛有金币，低岛稳。”

If a change does not serve aerial commit, it does not ship this phase. Roll stays meadow’s lesson. Do not paste a low-gate onto the sky.

## compile()

Still a bot helper. Player intent is the authored pads, rings, and (if needed) a hint string — not a new compiler.

## Tuning log

Every change: before / after / reason in `docs/gameplay/sky-tuning-log.md`.
