# Implement — Phase 5

## Slice 1 (menu chrome) — done

Hub rail/dock, Settings two-col, Gacha/Victory right dock, CameraRig horizontal offset.

## Slice 2 (gameplay HUD) — done

Compact chip row, hide standings, pad scale 0.8, pause actions row. Pad logic unchanged.

## Slice 3 (play footer + smoke) — done

PlayPane cards in the dock body; PLAY in `.hub-panel-foot` so the grid cannot scroll the CTA away. `:has(.hub-play-grid)` body overflow hidden. Smoke: 852×393 play/victory, 932×430 home/play, portrait settings.
