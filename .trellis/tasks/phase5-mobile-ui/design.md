# Design — Phase 5

Shared overlay components + CSS regions. See `docs/ui/ui-layout-spec.md`.

Portrait: compact the existing bottom nav/sheet (do not invent a second IA).  
Compact landscape: `.hub-nav` rail, `.hub-panel` right dock, `.hub-cta` right PLAY.

Camera: horizontal view-offset when a right dock is up; no Canvas remount.
