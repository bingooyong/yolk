# Implement — Phase 9 Sky S-curve

1. `Platform.lane` + infer + `compile()` filter
2. `extend(..., lane?)` / `plat(..., lane?)`
3. `skyJump()` S offsets + HIGH_X relative + pickups/gate/rings/rec/stairs
4. `nextSafePadX` in `spatial.ts`; CameraRig look-at lead
5. sky-layout tests: weave + hopB still a waypoint + high still not
6. `lessons.md` / spec rule 18 / level4-sky ASCII
7. `tsc` + `test:visual` + naive still dies first pit
