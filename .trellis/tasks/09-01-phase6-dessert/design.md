# Design — 甜品工厂

## Change boundary

Only `dessert()` in `levels.ts`, new `dessert-layout.test.ts`, docs, visual script. Do not touch other courses.

## Why this combo

`EggRacer` ice grip is 1.55. Roll/dash overwrite `vx/vz`. Walking on chocolate slides; roll/boost commit.

Safe line stays `x=0` cake+chocolate so bots compile it. Gates are player-only. Highway `x≈6.8` is `side`.

## Forbidden gadgets

spinner, hammer, pendulum, bounce, conveyor, mover, wind, traps.
