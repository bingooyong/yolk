# Design — 云端竞速

Wind was `vx += force * dt` after movement capture; ground grip erased it.

XZ force is added to `hx/hz` as m/s. Y stays accel. Leave the volume → walk speed.

Crosswind 1 is the fail beat. Tailwind is the reward. Jet is the greedy side line.
